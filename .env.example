# one model for every condition (CLAUDE.md rule 4). Written into every results/ row.
MODEL=gpt-4o-mini
OPENAI_API_KEY=sk-...
# optional local backend for the fine-tuned student (phase 3)
LOCAL_BASE_URL=http://localhost:8000/v1
# CLAUDE.md rule 8's ceiling, enforced in src/llm.py before each call is issued.
MAX_SPEND_USD=5.00
